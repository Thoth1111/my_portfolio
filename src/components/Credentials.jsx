import React from 'react';

const Credentials = () => {
    return (
        <section className="d-flex flex-column align-items-center justify-content-center pt-5">
            <h1 className="heading my-5" id="credentials">Credentials</h1>
        </section>
    );
};

export default Credentials;